PROFILE_IMAGE_BACKEND = {
    "class": DEFAULT_FILE_STORAGE,
    "options": {
        "bucket_name": "{{ MINIO_BUCKET_NAME }}",
        "querystring_auth": False,
        "location": PROFILE_IMAGE_BACKEND["options"]["location"].lstrip("/"),
    },
}
PROGRAM_IMAGE_BACKEND = {
    "class": DEFAULT_FILE_STORAGE,
    "options": {
        "bucket_name": "{{ MINIO_BUCKET_NAME }}",
        "querystring_auth": False,
        "location": PROGRAM_IMAGE_BACKEND["options"]["location"].lstrip("/"),
    },
}
BANNER_IMAGE_BACKEND = {
    "class": DEFAULT_FILE_STORAGE,
    "options": {
        "bucket_name": "{{ MINIO_BUCKET_NAME }}",
        "querystring_auth": False,
        "location": BANNER_IMAGE_BACKEND["options"]["location"].lstrip("/"),
    },
}
ORGANISATION_LOGO_BACKEND = {
    "class": DEFAULT_FILE_STORAGE,
    "options": {
        "bucket_name": "{{ MINIO_BUCKET_NAME }}",
        "querystring_auth": False,
        "location": ORGANISATION_LOGO_BACKEND["options"]["location"].lstrip("/"),
    },
}
DOCUMENT_STORAGE_BACKEND = {
    "class": DEFAULT_FILE_STORAGE,
    "options": {
        "bucket_name": "{{ MINIO_BUCKET_NAME }}",
        "querystring_auth": False,
        "location": DOCUMENT_STORAGE_BACKEND["options"]["location"].lstrip("/"),
    },
}
H5PXBLOCK_STORAGE = {
    "storage_class": DEFAULT_FILE_STORAGE,
    "settings": {
        "bucket_name": "{{ MINIO_BUCKET_NAME }}",
        "querystring_auth": False,
    },
}
AI_COURSE_FILES_STORAGE_BACKEND = {
    "storage_class": DEFAULT_FILE_STORAGE,
    "settings": {
        "bucket_name": "{{ MINIO_BUCKET_NAME }}",
        "querystring_auth": False,
        "location": AI_COURSE_FILES_STORAGE_BACKEND["options"]["location"].lstrip("/"),
    },
}